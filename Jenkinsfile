node{
    if (env.BRANCH_NAME == 'pipeline_git_3'){
        stage('Build from pipeline_git_3'){
            echo "Build is triggered"
            sh 'env'
            //test1
            //test
        }
    }

}