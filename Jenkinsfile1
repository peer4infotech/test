pipeline{
    agent none
    options {
        skipDefaultCheckOut()
        timestamps()
        overrideIndexTriggers(true)
    }
    stages {
        stage("Build"){
            steps{
                echo "HelloWorld"
            }
        }
    }
}